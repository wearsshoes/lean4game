import { LevelInfo } from "../../state/api";
import * as React from 'react';

export function DragDropInterfaceWrapper({ world, level, data, worldSize }: { world: string, level: number, data: LevelInfo, worldSize: number }) {
  return <div>DragDropInterfaceWrapper</div>;
}
